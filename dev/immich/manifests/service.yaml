---
apiVersion: v1
kind: Service
metadata:
  name: homepage
  labels:
    name: immich
  annotations:
spec:
  type: ClusterIP
  ports:
    - port: 2283
      targetPort: http
      protocol: TCP
      name: http
  selector:
    name: immich